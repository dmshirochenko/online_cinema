from setuptools import setup

setup(
    name="ml_content",
    version="0.1.0",
    description="ML Lib for Content-based RecSys",
    packages=["ml_content"],
    install_requires=[
        "numpy",
    ],
)
